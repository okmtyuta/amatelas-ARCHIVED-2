export const standardMaterialPasswordTextfieldPrefixed = (target?: string) => {
  const prefix = 'AMUI-components_material-password-textfield_standard_'
  if (target) {
    return `${prefix}${target}`
  }

  return prefix
}
export const outlinedMaterialPasswordTextfieldPrefixed = (target?: string) => {
  const prefix = 'AMUI-components_material-password-textfield_outlined_'
  if (target) {
    return `${prefix}${target}`
  }

  return prefix
}
export const filledMaterialPasswordTextfieldPrefixed = (target?: string) => {
  const prefix = 'AMUI-components_material-password-textfield_filled_'
  if (target) {
    return `${prefix}${target}`
  }

  return prefix
}
